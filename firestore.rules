rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Single-user app behind a passcode wall + Firebase anonymous auth.
    // The app signs in anonymously on load. These rules require auth on all reads/writes.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
